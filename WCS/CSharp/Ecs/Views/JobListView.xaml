<UserControl x:Class="Ecs.Views.JobListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel>
        <DockPanel DockPanel.Dock="Top" Margin="4">
            <Label Content="상태 필터:"/>
            <ComboBox x:Name="CbStatus" Width="100" SelectionChanged="CbStatus_Changed">
                <ComboBoxItem Content="전체" Tag="-1" IsSelected="True"/>
                <ComboBoxItem Content="대기중"   Tag="0"/>
                <ComboBoxItem Content="실행중"   Tag="2"/>
                <ComboBoxItem Content="완료"     Tag="4"/>
                <ComboBoxItem Content="에러"     Tag="5"/>
            </ComboBox>
            <Button Content="취소" Margin="8,0,0,0" Click="BtnCancel_Click"/>
            <Button Content="새로고침" Margin="4,0,0,0" Click="BtnRefresh_Click"/>
        </DockPanel>
        <ListView x:Name="JobListCtrl" FontSize="11"
                  SelectionMode="Single">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Lugg#"    Width="80"  DisplayMemberBinding="{Binding LuggNum}"/>
                    <GridViewColumn Header="타입"      Width="80"  DisplayMemberBinding="{Binding JobTypeStr}"/>
                    <GridViewColumn Header="상태"      Width="80"  DisplayMemberBinding="{Binding StatusStr}"/>
                    <GridViewColumn Header="출발"      Width="140" DisplayMemberBinding="{Binding SrcStr}"/>
                    <GridViewColumn Header="목적지"    Width="140" DisplayMemberBinding="{Binding DstStr}"/>
                    <GridViewColumn Header="SC"        Width="40"  DisplayMemberBinding="{Binding ScIndex}"/>
                    <GridViewColumn Header="생성시간"   Width="140" DisplayMemberBinding="{Binding CreateTimeStr}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </DockPanel>
</UserControl>
