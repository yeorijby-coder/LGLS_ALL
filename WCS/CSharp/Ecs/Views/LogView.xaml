<UserControl x:Class="Ecs.Views.LogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel>
        <DockPanel DockPanel.Dock="Top" Margin="4">
            <Label Content="레벨 필터:"/>
            <ComboBox x:Name="CbLevel" Width="100" SelectionChanged="CbLevel_SelectionChanged">
                <ComboBoxItem Content="전체" Tag="-1" IsSelected="True"/>
                <ComboBoxItem Content="DEBUG"   Tag="0"/>
                <ComboBoxItem Content="INFO"    Tag="1"/>
                <ComboBoxItem Content="WARNING" Tag="2"/>
                <ComboBoxItem Content="ERROR"   Tag="3"/>
            </ComboBox>
            <Label Content="검색:" Margin="8,0,0,0"/>
            <TextBox x:Name="TxtSearch" Width="160" TextChanged="TxtSearch_TextChanged"/>
            <Button Content="지우기" Margin="8,0,0,0" Click="BtnClear_Click"/>
            <CheckBox x:Name="ChkAutoScroll" Content="자동 스크롤" IsChecked="True" Margin="8,0,0,0" VerticalAlignment="Center"/>
        </DockPanel>
        <ListView x:Name="LogListView" FontFamily="Consolas" FontSize="11">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="시간"     Width="160" DisplayMemberBinding="{Binding TimeStr}"/>
                    <GridViewColumn Header="레벨"     Width="70"  DisplayMemberBinding="{Binding LevelStr}"/>
                    <GridViewColumn Header="카테고리"  Width="80"  DisplayMemberBinding="{Binding Category}"/>
                    <GridViewColumn Header="메시지"    Width="600" DisplayMemberBinding="{Binding Message}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </DockPanel>
</UserControl>
