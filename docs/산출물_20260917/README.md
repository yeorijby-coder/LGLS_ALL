# 산출물 (2026-09-17) - ECS 화면설계서 V1.2

| 파일 | 내용 |
|---|---|
| 06_ECS_화면설계서.pptx | 33장. 현재 실행 중인 `EXE_NEWUI\WCS_CLIENT\Ecs.exe`(Build 2026.09.17 08:18:34)와 `Ecs.ini` 기준 |

## 09-09 판(V1.1)에서 바뀐 점

- **메인 화면 전체화면 캡처** (1920×1080 최대화) 한 장 + 영역 구성 장
- **새 배치 MAIN_UI=2** : 왼쪽에 통신 상태·범례·설비 반송·작업정보 고정. 종전 레이아웃 안 범례 표와 [판넬 보기] 장은 뺐다
- **리본 정리** : INI 열기(`INI_MENU=0`), 반자동 TEST 그룹(`SEMITEST_MENU=0`), 설비 대화상자 확대 버튼(`ZOOM_BTN=0`) 숨김
- **크레인·RGV 색 범례 정합** : 작업없음 = 범례 정상색, 미가동 = 범례 수동색
- **레이아웃 그룹 이름 정정** : `EcsLayout1.xml` 의 SC / RTV
- 우클릭·휠은 현재 사용하지 않음(추후 정의), 리본 [작업정보] 는 별도 창(의도된 동작)
- 부록 : Ecs.ini 화면 설정 현재값, 설계 결정·변경 이력
- 서버 프로그램 장 : 설비 통신 속도 옵션(WCS_DB.INI), 완료 재보고 간격(EcsComA.ini `RE_REPORT_09_SEC`) 반영

## 캡처

`shots/` 에 있다.

- 2026-09-17 새로 찍음 : `main_full`, `ribbon_*`, `left_*`, `layout`, `cv_0`, `sc_0`, `rtv_0`, `legend_0`
- 09-09 판 그대로 : 그 밖의 대화상자(화면 구성 변경 없음)

캡처할 때 Client 창을 최대화한 상태여야 한다. 창 크기를 바꾸면 Client 가 왼쪽 칸 폭(`MAIN_UI_LEFT_W`)을
비례해 다시 저장하므로, 최대화 기준값 531 을 확인한다.

## 다시 만들기

```
cd gen
python gen_screen_pptx.py
```
