# 9월 15일(화) 현장 점검 순서

크레인 1호기 출고 정체 · 크레인 상태창 미표시 · RTV 에러 해제 · 트래킹 중복

**배포본 `EXE\`** : Client `Ecs.exe` 09-12 17:06 / `WCS_TASK_CV.exe` 09-12 17:06 + `WCS_DB.INI` 22:11 / `IO_TASK_SEMI_FINISH.exe` 09-11 17:25
**되돌림용** : `EXE_20260911_AM\` (오전 배포본 그대로)

> 오늘 가려야 할 것 하나. 1호기 크레인이 104에 내려놓은 뒤 **Unload Complete Ack(M1538)** 가 PLC에 박히는가,
> 박히면 C/V#2 벨트가 104→103으로 도는가. 방향 워드(D301)는 어제 메모로 확인됐으므로 오늘은 보지 않는다.
> PLC 담당자와 함께 **2번**을 하는 것이 핵심이고, 나머지는 로그 회수다.

## 1. 교체 · 기동 (서버 → 키오스크, 10분)

- [ ] 전부 내린다: Client(두 대) → HOST_TASK → IO_TASK → WCS_TASK_CV
- [ ] `EXE\TASK\WCS_TASK_CV\WCS_TASK_CV.exe`, `EXE\TASK\IO_TASK\IO_TASK_SEMI_FINISH.exe`, `EXE\WCS_CLIENT\Ecs.exe` + `DciLib.dll EcsLib.dll XmlLib.dll` 교체 (키오스크도 Client 교체)
- [ ] **ini는 현장 것을 그대로 둔다.** 새 키(`UI_TRACE`, `LOADBIT_GATE`, `CV_GLOBAL_ALARM`)는 없어도 기본값이 새 동작이다
- [ ] 기동 순서: **WCS_TASK_CV → IO_TASK → HOST_TASK → Client**

되돌려야 하면 `EXE_20260911_AM\` 의 같은 파일로 덮으면 오전 상태다.

## 2. 기동 직후 확인 (5분)

- [ ] WCS_TASK_CV 화면 로그에 `[CvAlarmCheck] 전 설비 공통 알람 처리 사용 안 함`
- [ ] `wcs_log_pgr` 에 `[VEH_SC] PLC 접속 성공 (…, 차량 5대)`, `[VEH_RTV] PLC 접속 성공`
- [ ] `OFF 기록 - 기동 직후 정합` 줄이 있으면 어제 굳어 있던 Ack 를 내린 것 — 정상, 몇 호기였는지만 적어 둔다
- [ ] Client 메인에서 C/V#2(103/104) 표시가 뜨고, 크레인 5대·RTV 위치가 움직인다

## 3. 1호기 출고 1건 — PLC 담당자 동석 (핵심, 20분)

담당자가 XG5000 에 **M785**(1호기 Unload Complete)와 **M1538**(그 Ack)을 띄운 상태에서 1호기 출고를 1건 보낸다.
크레인이 104 에 내려놓는 순간부터 이 순서가 몇 초 안에 지나가야 한다.

```
M785 ↑ (크레인: 내려놨다)
  → 0.3초 안  M1538 ↑ (WCS)      wcs_log_pgr : [VEH_SC] VEHICLE@11 UNLOAD_COMPLETE_ACK (M1538) ON 기록
  → M785 ↓ (크레인: 확인했다)
  → M1538 ↓ (WCS)                               ... OFF 기록
  → C/V#2 벨트 기동 104 → 103
```

| 보이는 것 | 뜻 | 다음 |
|---|---|---|
| **A** `ON 기록` → 몇 초 안 `OFF 기록`, **벨트가 돈다** | 원인은 지워지던 Ack 였다 | 끝. 출고 몇 건 더 돌려 재현 없음을 본다 |
| **B** `ON 재기록 - 외부 덮어쓰기 의심` 이 반복 | 우리가 올리는 족족 누군가 내린다 (공통 알람은 껐음) | 담당자: "M1538 을 리셋하는 래더가 있습니까?" |
| **C** `ON`→`OFF` 깨끗한데 **벨트가 안 돈다** | 크레인 핸드셰이크는 끝났고 벨트가 다른 것을 기다린다 | 담당자: "C/V#2 가 기동하는 조건이 정확히 무엇입니까?" C/V 쪽 Load Complete #2 Ack 로그와 대조 |

**또 104 에서 멈추면**
- **M785 가 ON** 이면 1초만 기다린다 — WCS 가 스스로 다시 쓴다(재기록 로그). 그래도 안 돌면 **S/C#1 상태창 → [확대] → 하역ACK 옆 [쓰기] → [예]** (Ack 를 크레인 스레드가 정식 경로로 다시 쓴다). 그래도면 담당자가 M1538 을 강제 ON 하고 위 순서가 이어지는지 본다.
- **M785 가 OFF** 면 Ack 를 켜도 의미가 없다. 담당자가 포트 인수 절차를 다시 태우거나 벨트를 수동으로 104→103 보낸다. **ECS 는 손댈 것 없다** — 화물이 103 에 닿으면 RGV 가 자동으로 나간다.

**로그 뽑기**
```sql
SELECT ins_dt, log_kor FROM wcs_log_pgr
 WHERE ins_dt >= '2026-09-15'
   AND (log_kor LIKE '%VEHICLE@11%' OR log_kor LIKE '%방향지시%' OR log_kor LIKE '%Load Complete #2%')
 ORDER BY ins_dt
```

## 4. 크레인 상태창 — 서버, 그다음 키오스크 (각 3분)

- [ ] 크레인을 한 번 클릭 → 안 뜨면 **평소대로 ESC**
- [ ] **RTV 도 한 번 클릭** (비교선)
- [ ] 두 대 모두 `실행폴더\LOG\` 안 `ECS_UI_20260915.log`, `ECS_SQL_2026091*.log`, `ECS_CONNECT_*.log` 회수

| 로그가 이렇게 끝나면 | 뜻 |
|---|---|
| `[ENV] os=6.x dwm=0 …` | 합성(DWM) 꺼진 구 OS — 그리기 쪽 원인이면 여기서 갈린다 |
| `[SC] CLICK` 만 있고 끝 (`ABORT …`) | 설비 매칭 실패 — ABORT 줄이 이유를 적어 준다 |
| `[SC] SHOW … visible=0` 또는 좌표가 화면 밖 | 창은 만들었는데 안 보이는 상태 |
| `[SC] REFRESH end` 없음 / `SQL1`·`SQL2` 수천 ms | DB 대기 |
| `[SKIN] ESC hide … visible=1 rect=… rgn=…` | ESC 누른 순간 창의 실제 상태 — **이 한 줄이 결정적** |

## 5. RTV 에러 해제 (5분)

- [ ] RTV 에러를 한 번 걸었다 현장에서 해제 → Client 의 RTV 에러 표시가 몇 초 안에 따라 꺼지는지
- [ ] `wcs_log_pgr` 에 `[VEH_RTV] VEHICLE@1 알람코드 워드가 0 → 에러 해제 반영`
- [ ] 안 꺼지면: PLC 가 해제 때 알람코드 워드(**D211**)를 0 으로 안 내리는 것 → 담당자 확인

## 6. 트래킹 중복 (27번, 지나가며)

- [ ] RTV 가 127(또는 111·125)에 내려놓은 뒤 IO_TASK 로그 `… 트랙에 작업번호 기록 요청` 이 **작업당 한 번**인지 (20초 안 재요청 없음)

## 7. PLC 담당자께 묶어서 물어볼 것

- **M1538**(1호기 Unload Complete Ack)을 **리셋하는 래더**가 있는가 — 있다면 어떤 조건에서
- **C/V#2 벨트가 104→103 으로 기동하는 조건**은 정확히 무엇인가 (방향 외에 크레인 Ack / Load Complete #2 Ack 를 보는가)
- 크레인이 Unload Complete 를 올리고 **Ack 를 기다리는 시간**(타임아웃)이 있는가, 넘기면 어떻게 되는가
- 완료 이벤트는 **Ack 받을 때까지 유지**되는가, 정해진 펄스 폭인가 (폴링이 놓칠 수 있는 쪽이면 래치 협의)
- RTV 알람 해제 시 **D211**(ALARM_SET_CODE)을 0 으로 내리는가
- (한 줄만) D301 에 넣는 값은 문자 `'1'`(0x31)이 맞는가

## 8. 회수해 올 것 (퇴근 전)

- [ ] `wcs_log_pgr` 오늘치 전체
- [ ] IO_TASK `ECS_LOG\IOTASK_SCHEDULER_20260915.log`
- [ ] WCS_TASK_CV 실행폴더의 `LOG\` 폴더
- [ ] Client 두 대의 `LOG\` 폴더
- [ ] 멈춤이 났다면 그 시각의 **M785 · M1538 · D301 값** (사진이나 메모)

---
ini 스위치 : `UI_TRACE=0` 추적 끔 · `LOADBIT_GATE=0` 크레인/RTV 색이 아예 안 뜰 때 · `CV_GLOBAL_ALARM=1` 옛 알람 처리로 복귀
